# Robolectric の既定の端末設定 (全テスト共通)。
# 既定の言語は en-rUS なので、アプリの既定言語 ja (values/ = ja) に合わせる。values-en を足しても
# 既存の日本語の文言を確かめるテストが英語に化けないようにするため。言語ごとの確認は
# テストクラスの @Config(qualifiers = "ko") で上書きする。
qualifiers=ja
